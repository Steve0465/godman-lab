name: skill_drive_normalize
version: "0.1.0"
description: Normalize drive filenames
entrypoint: examples.skills.drive.skill_drive_normalize.main:run
inputs:
  - metadata
outputs:
  - filename
permissions: []
